# PLIC Handoff Status

The prior PLIC handoff belonged to the private historical snapshot and is not part of this public source candidate. It is not evidence that an active public PLIC implementation is complete.

If PLIC work resumes, first define and approve the interrupt-controller behavior in `spec/`, then implement RTL/FW and independent DV through the normal public-repository workflow. No private migration path or historical worktree is required to understand this status.
